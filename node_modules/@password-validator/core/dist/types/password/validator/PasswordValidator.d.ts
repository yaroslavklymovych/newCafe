import { Validator } from "./Validator.js";
import { ValidationResult } from "./ValidationResult.js";
import { ValidatorCategory } from "./ValidatorCategory.js";
export declare abstract class PasswordValidator implements Validator {
    private readonly rule;
    private readonly validatorCategory;
    protected constructor(category: ValidatorCategory, passwordRule: number);
    passwordRule(): number;
    category(): ValidatorCategory;
    conflictsWith(validator: PasswordValidator): Array<string>;
    abstract validate(password: string): ValidationResult;
}
