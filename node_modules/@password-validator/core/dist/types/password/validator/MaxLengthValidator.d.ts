import { ValidationResult } from "./ValidationResult.js";
import { PasswordValidator } from "./PasswordValidator.js";
export declare class MaxLengthValidator extends PasswordValidator {
    private maxCount;
    constructor(passwordRule: number);
    validate(password: string): ValidationResult;
    conflictsWith(validator: PasswordValidator): Array<string>;
    private validateLengthExpanderConflicts;
    private handleLengthMinimizerConflicts;
}
