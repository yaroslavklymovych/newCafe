import { ValidatorManager } from "../../validator/ValidatorManager.js";
import { ValidationResult } from "../../validator/ValidationResult.js";
import { PasswordValidator } from "../../validator/PasswordValidator.js";
import { FluentPasswordValidator } from "../fluent/FluentPasswordValidator.js";
export declare class PasswordValidatorManager implements ValidatorManager {
    private readonly registeredValidators;
    private constructor();
    static standard(): PasswordValidatorManager;
    static fluent(): FluentPasswordValidator;
    register(...validators: Array<PasswordValidator>): void;
    private verifyEachValidatorAgainstThemselves;
    private verifyAgainstOtherValidators;
    private registerValidatorWithNoConflict;
    private handleConflicts;
    private hasConflict;
    validators(): Array<PasswordValidator>;
    validate(password: string): ValidationResult;
}
