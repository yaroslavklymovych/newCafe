"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.MinLengthValidator = void 0;
const ValidationResult_js_1 = require("./ValidationResult.js");
const PasswordValidator_js_1 = require("./PasswordValidator.js");
const ValidatorCategory_js_1 = require("./ValidatorCategory.js");
class MinLengthValidator extends PasswordValidator_js_1.PasswordValidator {
    constructor(passwordRule) {
        super(ValidatorCategory_js_1.ValidatorCategory.LENGTH_MINIMIZER, passwordRule);
    }
    validate(password) {
        if (password.length < this.passwordRule()) {
            const validationMsg = `must not be less than ${this.passwordRule()} minimum characters.`;
            return new ValidationResult_js_1.ValidationResult(false, Array.of(validationMsg));
        }
        return new ValidationResult_js_1.ValidationResult(true, Array.of());
    }
    conflictsWith(validator) {
        const conflictMsg = Array.of();
        switch (validator.category()) {
            case ValidatorCategory_js_1.ValidatorCategory.TOTAL_LENGTH_LIMITER:
                if (validator.passwordRule() < this.passwordRule()) {
                    conflictMsg.push("minLength cannot be greater than maxLength");
                }
                break;
            default:
                return Array.of();
        }
        return conflictMsg;
    }
}
exports.MinLengthValidator = MinLengthValidator;
